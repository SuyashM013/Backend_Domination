module.exports.userController = (req, res) => {
    // res.send('Hii from user and inside the controllers. this random number from middlewares'+ req.randomnumber).toString();

    res.render('create');
    
}